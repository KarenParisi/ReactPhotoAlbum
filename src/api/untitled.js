import React from 'react';
import data from "api/data"

export default React.createClass({
	render: function () {
		return (
			{this.data.results.map(function(item){
				return results.Imgaes;
			}}
		)
	}
})